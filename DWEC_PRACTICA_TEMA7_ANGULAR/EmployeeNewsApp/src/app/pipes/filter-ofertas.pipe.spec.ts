import { FilterOFertasPipe } from './filter-ofertas.pipe';

describe('FilterOFertasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOFertasPipe();
    expect(pipe).toBeTruthy();
  });
});
